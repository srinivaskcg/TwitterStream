# goto the downloaded directory and execte the following command

# starts a node and runs with projections enabled and.. sets the stats projections(which is by default triggering to 5 min-- this is to reduce the overhead).
sh run-node.sh --run-projections=all --stats-period-sec=300
